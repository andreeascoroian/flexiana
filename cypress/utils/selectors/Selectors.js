let homepageSelectors = {
    RESTART: '.gameWrapper .footnote a:first-of-type',
    AGREE_PRIVACY: '[mode="primary"]',
    ORANGE_PIECES: '#board [src="you1.gif"]',
    MESSAGE: '#message'
}

module.exports = {
    homepageSelectors
}